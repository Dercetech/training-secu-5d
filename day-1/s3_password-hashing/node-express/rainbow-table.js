"use strict";

const MAX_LENGTH = 4;

function findPassword(targetHash, minLength, maxLength) {
  // LABO : cherchez les mots un par un entre minLength et maxLength.
  // Retour attendu : { password: motOuNull, elapsed_ms: nombre }.
  return null;
}

module.exports = { findPassword, MAX_LENGTH };
