"""Exercice : implémenter la recherche SHA-256 séquentielle."""

from __future__ import annotations

MAX_LENGTH = 4


def find_password(
    target_hash: str,
    min_length: int,
    max_length: int,
) -> dict[str, str | float | None] | None:
    """Return None until the learner implements the exercise."""

    # LABO : cherchez les mots un par un entre min_length et max_length.
    # Retour attendu : {"password": mot_ou_none, "elapsed_ms": nombre}.
    return None
